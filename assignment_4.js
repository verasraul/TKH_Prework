// Create a loop that compares the array of numbers below to the number 25 and print "under" if it is under and "over" if it is over.

var array = [1,45,32,21,5,17,43,93];


for (x=0;x<array.length;x++){ 
  if(array[x]<25) 
	console.log("under") 
	else { 
	console.log("over") 
	} 
};